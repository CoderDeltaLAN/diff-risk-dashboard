#!/usr/bin/env bash
src="$(bin/_dr_common.sh "${1-}")" || exit $?
me="$(basename "$0")"
case "$me" in
  drt)  f=table;;
  drb)  f=bar;;
  drj)  f=json;;
  drmd) f=md;;
esac
exec poetry run diff-risk "$src" -f "$f" --no-exit-by-risk
